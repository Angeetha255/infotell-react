# Email Verification Server Configuration
PORT=5007

# Nodemailer SMTP Configuration
# For Gmail: use App Password (not your Gmail password)
# Enable 2FA on Google Account → App Passwords → Generate one
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=infotech.info.0205@gmail.com
SMTP_PASS=jqki xqdc ugju rqoq

# The email address that sends verification emails
EMAIL_FROM="Infotell Verification <infotech.info.0205@gmail.com>"

# Frontend URL — used to build the magic link redirect URL
FRONTEND_URL=http://localhost:3000

# Secret used to sign/hash tokens (use a long random string in production)
TOKEN_SECRET=infotell-review-verification-secret-2024-change-in-production
